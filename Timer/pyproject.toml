[build-system]
requires = ["setuptools"]

[project]
name = "limnoria-timer"
version = "0.0.0"
authors = [
    { name = "Terje Hoås" },
]
readme = "README.md"
dependencies = [
    "limnoria",
]
classifiers = [
    'Environment :: Plugins',
    'Programming Language :: Python :: 3',
    'Topic :: Communications :: Chat',
]

[project.entry-points.'limnoria.plugins']
Timer = "limnoria_timer"

[tool.setuptools.package-dir]
"limnoria_timer" = "."

[tool.setuptools.package-data]
"limnoria_timer" = ["locales/*.po"]
