Supply a username and get the latest tweet(s). Or an ID to a tweet. Or search on twitter.
Does not require a user account or apikey or anything like that. Just point and shoot.

This plugin does NOT relay tweets in real time. It only fetches data from Twitter
when commands are called.
